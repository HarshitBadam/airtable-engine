/**
 * Shared UI constants
 * Extracted magic numbers for menu positioning, timing, etc.
 */

// Menu dimensions
export const MENU_WIDTH = 240;
export const MENU_HEIGHT = 260;

// Viewport safety margins
export const VIEWPORT_EDGE_BUFFER = 16;
export const VIEWPORT_BOTTOM_BUFFER = 8;

// Timing (milliseconds)
export const HOVER_RESET_DELAY = 50;

// Sidebar breakpoints
export const SIDEBAR_AUTO_COLLAPSE_WIDTH = 600;
